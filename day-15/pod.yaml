apiVersion: v1
kind: Pod
metadata:
  name: nginx-kyverno-validation
spec:
  containers:
  - name: nginx-kyverno-validation
    image: nginx:latest
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
      requests:
        memory: "64Mi"
        cpu: "250m"
